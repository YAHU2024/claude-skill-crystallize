---
name: crystallize
description: 【全局技能】深度复盘当前对话的解题路径、失败尝试与影响范围，追加沉淀到当前项目的 docs/THOUGHTS.md 中。
disable-model-invocation: false
---

# 终极思路沉淀智能体技能 (Global Crystallize Thought Process)

当你（Claude）发现解决了一个复杂的代码问题，或者用户在任意项目中主动输入了 `/crystallize` 时，请触发本技能。

## 执行步骤：

1. **精准分类判定**：
   从以下标签中选择最契合的 **1-2个**：
   `[Bug修复]`, `[性能优化]`, `[架构设计]`, `[环境配置]`, `[工程化]`, `[AI工作流]`

2. **多维提取与格式化**：
   严格按照以下 Markdown `<details>` 模板组织内容。务必提取出技术栈关键词、复用价值，以及**真实的失败尝试**。

3. **目录与文件准备 (全局环境关键步骤)**：
   由于你运行在各种不同的项目中，请务必先确保目录存在：
   `mkdir -p docs`
   若创建失败（如权限不足），向用户报告具体原因并中止。

4. **持久化追加**：
   将生成的 Markdown 文本**追加（Append）**到当前项目根目录的 `docs/THOUGHTS.md` 文件末尾（用两行空行隔开，**绝对不可覆盖历史记录**）。

5. **VS Code 联动**：
   写入完成后，在终端执行 `code -r docs/THOUGHTS.md` 唤起用户的编辑器。

---

## 写入模板标准

###  [YYYY-MM-DD] 【类型标签】一句话概括核心问题或变更
> **核心标签**: #分类标签
> **关键词**: #技术栈1 #技术栈2
> **复用价值**: ⭐️⭐️⭐️⭐️☆

<details>
<summary> 点击展开深度技术复盘（包含失败尝试与影响范围）</summary>

####  问题现象 & 影响范围
- **背景现象**：简述遇到了什么 Bug、报错信息或核心需求。
- **波及模块**：修改了哪些模块？是否涉及破坏性变更或影响生产环境？

#### ❌ 失败尝试 (Rejected Solutions)
- **尝试 1**：[做了什么尝试] -> **失败原因**：[为什么无效/误判]
*(注：如果一次性成功，此项可写"无")*

####  根本原因 (Root Cause)
- **底层机制**：导致问题的核心逻辑、协议限制或框架机制是什么？

####  最终有效解法
- **核心路径**：最终是如何彻底修复或实现的。
- **关键代码/命令**：
  ```bash
  # 留存最关键的配置命令、diff 或修复代码
  ```

####  经验教训 (Lessons Learned)
- **核心认知**：这次解决过程中获得的关键洞察。
- **可复用模式**：类似场景可直接套用本解法。

</details>

---

## 约束

- 只输出符合上述模板的内容，禁止在模板之外掺杂闲聊、自我评价或无关对话片段。
- 若用户未提供足够信息（如不知道问题分类），向用户追问关键细节后再生成记录。
